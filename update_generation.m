function [ newGen ] = update_generation( Gen, T, Nodes, parentType, rnd, rep )
% updates the generation
    
    newGen = Gen + 1;
%disp('parent type');
%disp(parentType);
%disp('gen');
%disp(newGen);
end

